import { Card } from "@mui/material";

export default function StorageCard() {
    return (
        <Card className="flex items-center justify-center grow">
            <span>StorageCard</span>
        </Card>
    );
}
